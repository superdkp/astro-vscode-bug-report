---
import { SOME_CONST } from "../lib/utils";

// BUG: with only this line, ts(6133) is reported on the import
return Astro.redirect(SOME_CONST);

// Switch to these two lines instead, and ts(6133) goes away
// const buggy = Astro.redirect(SOME_CONST);
// return buggy;
---

